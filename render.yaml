services:
  - type: web
    name: motioninsightai
    runtime: docker
    dockerfilePath: motioninsight/backend/Dockerfile
    envVars:
      - key: GROQ_API_KEY
        sync: false   # set manually in the Render dashboard — never commit this value
