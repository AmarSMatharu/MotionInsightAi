services:
  - type: web
    name: motioninsightai
    runtime: python
    rootDir: motioninsight/backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 8000
